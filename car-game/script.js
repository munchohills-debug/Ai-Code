const c=document.getElementById('game'),x=c.getContext('2d');let car,traffic,score,running,last,spawn,best=+localStorage.bestTurbo||0;const keys={};document.getElementById('best').textContent=best;const road={x:90,w:300};function reset(){car={x:225,y:600,w:30,h:58,vx:0,vy:0};traffic=[];score=0;spawn=0;running=true;last=performance.now();requestAnimationFrame(loop)}function drawRoad(){x.fillStyle='#16202a';x.fillRect(0,0,480,720);x.fillStyle='#26313b';x.fillRect(road.x,0,road.w,720);x.fillStyle='#d9d9d9';x.fillRect(road.x-7,0,7,720);x.fillRect(road.x+road.w,0,7,720);x.strokeStyle='#ffffff66';x.lineWidth=5;x.setLineDash([35,35]);let off=(score*2)%70;x.lineDashOffset=off;for(let i=1;i<3;i++){x.beginPath();x.moveTo(road.x+i*100,0);x.lineTo(road.x+i*100,720);x.stroke()}x.setLineDash([])}function carDraw(o,color){x.fillStyle=color;x.beginPath();x.roundRect(o.x,o.y,o.w,o.h,8);x.fill();x.fillStyle='#aeeaff';x.fillRect(o.x+6,o.y+9,o.w-12,15);x.fillStyle='#111';x.fillRect(o.x+4,o.y+o.h-12,7,5);x.fillRect(o.x+o.w-11,o.y+o.h-12,7,5)}function spawnCar(){let lanes=[115,215,315];traffic.push({x:lanes[Math.floor(Math.random()*3)],y:-70,w:30,h:58,s:3+Math.random()*2.5,c:['#ffca28','#9c6cff','#35e0a1'][Math.floor(Math.random()*3)]})}function collide(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y}function loop(t){if(!running)return;let dt=Math.min((t-last)/16.67,2);last=t;score+=dt;spawn-=dt;if(spawn<=0){spawnCar();spawn=Math.max(18,45-score/100)}let steer=(keys.ArrowLeft||keys.a?-1:0)+(keys.ArrowRight||keys.d?1:0);car.x+=steer*5*dt;car.x=Math.max(road.x+10,Math.min(road.x+road.w-car.w-10,car.x));for(let o of traffic){o.y+=(o.s+score/700)*dt;if(collide(car,o)){running=false;best=Math.max(best,Math.floor(score));localStorage.bestTurbo=best;document.getElementById('best').textContent=best;setTimeout(()=>alert(`CRASH! Score: ${Math.floor(score)}\nPress START RACE to try again.`),10)}}traffic=traffic.filter(o=>o.y<750);drawRoad();for(let o of traffic)carDraw(o,o.c);carDraw(car,'#ff3d5a');document.getElementById('score').textContent=Math.floor(score);document.getElementById('speed').textContent=Math.floor(80+score/3);requestAnimationFrame(loop)}document.addEventListener('keydown',e=>{keys[e.key]=true;if(['ArrowLeft','ArrowRight','ArrowUp','ArrowDown',' '].includes(e.key))e.preventDefault()});document.addEventListener('keyup',e=>keys[e.key]=false);document.getElementById('startBtn').onclick=reset;document.querySelectorAll('.controls button').forEach(b=>{let k=b.dataset.key;b.onpointerdown=e=>{e.preventDefault();keys[k]=true};b.onpointerup=b.onpointercancel=()=>keys[k]=false});drawRoad();car={x:225,y:600,w:30,h:58};carDraw(car,'#ff3d5a');
